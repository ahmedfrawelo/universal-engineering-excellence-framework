# Usage

Load only the boot loader and core system before engineering tasks. Use the master loader as a selector for relevant modules, then inspect the project, detect architecture and tools, plan, implement, validate, and report compact UEEF evidence.
