# UEEF 2.25.6

Release date: 2026-08-04

## Summary

This patch release supersedes `v2.25.5` after GitHub Actions exposed a stale Unix preflight assertion for Quick frontend work. The current frontend production contract legitimately selects `frontend-design` for material frontend mutations, so the Unix test now only rejects unrelated critique/product-intelligence directors.

## Fixed

- Updated `scripts/test-ueef-task-preflight.sh` to allow `frontend-design` in Quick frontend mutation routes when design-production applies.
- Continued to reject unrelated `impeccable` and `ui-ux-pro-max` stacking for Quick frontend work.

## Verification

- Full framework validation must pass on GitHub Actions before publishing the GitHub Release object.
