# UEEF v1.4.3

Deep-audit patch adding the source loader contract.

## Changes

- Added root `UEEF-LOADER.md` so a clone contains the portable loader contract before installation.
- Installers and runtime synchronization continue to generate resolved Codex-specific paths under `CODEX_HOME/ueef/codex`.
- Validation now requires the source loader template on Windows and Unix.
- No change to the compact runtime contract or profile readiness policy.
