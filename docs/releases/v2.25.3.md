# UEEF 2.25.3

Release date: 2026-08-04

## Summary

This patch release supersedes `v2.25.2` after GitHub Actions exposed a stale Unix route-test expectation. It preserves the completed 2.25.x runtime-enforcement payload and keeps the offline viewer asset normalization fix.

## Fixed

- Aligned `scripts/test-agent-route.sh` with the current PowerShell route contract for low default reasoning, T4 medium release routing, and current-model constrained T2 routes.
- Kept `vis-network.min.js` marked as `-text` so its pinned SHA384 remains stable across Windows and Unix checkout.

## Verification

- `scripts/test-agent-route.ps1` passed locally.
- `scripts/test-release-consistency.ps1` passed for `2.25.3`.
- Full framework validation must pass on GitHub Actions before publishing the GitHub Release object.
