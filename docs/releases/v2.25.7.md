# UEEF 2.25.7

Release date: 2026-08-04

## Summary

Patch release for the managed Codex runtime enforcement series after GitHub Actions exposed that the compact generated Codex `AGENTS.md` block exceeded the precedence-only size budget.

## Fixed

- Reduced the managed Codex `AGENTS.md` runtime block while preserving the routing, browser, evidence, and completion contract required by status checks.
- Kept runtime-hardening status expectations aligned with the compact `route rationale` contract.

## Verification

- `scripts/test-runtime-hardening.ps1`
- `scripts/test-release-consistency.ps1`
- `scripts/test-ueef-task-preflight.ps1`
- `scripts/test-agent-route.ps1`
- `scripts/validate-framework.ps1`