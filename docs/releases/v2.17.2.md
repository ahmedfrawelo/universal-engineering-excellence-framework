# UEEF 2.17.2

Release date: 2026-07-24

- Fixed GitHub Actions Windows validation so `CODEX_HOME` uses the runner's temporary directory rather than assuming the local `E:` drive exists.
- Made Codex-home and repository-relative PowerShell defaults resolve after script startup, which is safe for direct invocation as well as nested validation.
- Restored the executable Git mode for `get-ueef-task-preflight.sh`, allowing the Unix preflight test to invoke it directly.
