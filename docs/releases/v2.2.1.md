# UEEF v2.2.1

Added user-owned Chrome connection recovery. When a browser connector exposes a blank or wrong tab, the agent must discover available tabs read-only, verify the matching user tab, and request that the user connect that existing tab when needed. It must not create another browser, profile, or login session.
