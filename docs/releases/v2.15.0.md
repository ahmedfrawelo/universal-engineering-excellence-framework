# UEEF 2.15.0

Release date: 2026-07-23

- Browser tasks now emit a mandatory `browserGate` with an allowlisted user-owned Chrome sequence and hard-fail guidance before any browser tool.
- Runtime synchronization guidance makes installed-loader drift visible after browser-policy changes.
- Spec validation accepts structured requirement and acceptance identifiers without weakening Ready-mode traceability.
- Project memory summaries are opt-in and limited to three decisions by default; evidence export can render a concise PR summary.
- Only Codex, Cursor, and generic adapters are represented as tested; Claude Code remains an unverified guidance example.
