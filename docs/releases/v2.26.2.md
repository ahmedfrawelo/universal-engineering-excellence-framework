# UEEF 2.26.2

Release date: 2026-08-08

## Highlights

- Allows `create_goal` and `update_goal` lifecycle controls before an actual model dispatch, avoiding a routing deadlock at task creation, completion, or blocking.
- Retains the independent lifecycle evidence guard, so goal completion still cannot bypass required evidence.

## Verification

- Managed-enforcement regression tests cover both pre-dispatch lifecycle access and evidence-gated completion.
- Full framework validation, release consistency, and GitHub Actions validation pass for the release payload.
