# UEEF 2.21.3

Release date: 2026-07-31

## Summary

This patch preserves the localized multi-line route format from `2.21.2` while restoring Windows PowerShell 5.1-safe runtime generation.

## Fixed

- Removed raw Arabic literals from the PowerShell string table that generates the active loader.
- Kept the generated instruction language-neutral: translate intent, tier, agent, and browser labels into the response language.
- Retained the exact Arabic labels in Markdown policy files, where UTF-8 text is safe.

## Verification

- PowerShell parses and executes the runtime generator successfully.
- Intent-fidelity validation rejects the obsolete pipe-separated route format.
- Full framework validation and release-consistency checks pass.
- Managed runtime synchronization completes and the active loader contains the new localized-list policy.
