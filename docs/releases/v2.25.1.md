# UEEF 2.25.1

Release date: 2026-08-04

## Summary

This patch release publishes the completed UEEF 2.25.x runtime-enforcement work on top of the already published `v2.25.0` tag. It finalizes executable model routing, frontend design production governance, fresh-context review evidence, stricter browser-control recovery, repository graph viewer validation, and optional capability-health handling.

## Added

- Added executable Codex App Server model catalog, dispatch, route-result recording, and thread-settings update helpers.
- Added pack 64 for frontend design production, including design contracts, Styleseed quality loops, Penpot workflow controls, and frontend execution evidence validation.
- Added fresh-context review templates, protocol documentation, validators, and regression tests.
- Added interactive repository graph viewer validation and vendored `vis-network` assets with retained license notices.

## Hardened

- Kept optional plugin and skill warnings visible without degrading overall UEEF health when no blocking diagnostics exist.
- Strengthened browser-control recovery wording, Chrome readiness checks, and same-profile task-tab policy.
- Expanded runtime sync, managed hooks, route recording, and framework validation coverage for the larger runtime payload.

## Verification

- `scripts/validate-framework.ps1` passed for the final source payload.
- `scripts/sync-runtime.ps1 -CodexHome "D:\shared folder\codex-home" -Agent codex -BackupRoot "D:\shared folder\codex-home-backups"` passed.
- `D:\shared folder\codex-home\ueef\codex\scripts\ueef-status.ps1` reported `Overall: ACTIVE`, `Runtime drift: PASS`, and `Runtime source revision: PASS`.
- Health diagnostics reported `status=PASS`, `warnings=3`, and `blockingWarnings=0`.

## Release note

`v2.25.0` already existed on the remote, so this release uses `v2.25.1` rather than moving the published tag.
