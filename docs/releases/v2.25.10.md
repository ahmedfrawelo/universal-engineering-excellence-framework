# UEEF 2.25.10

Release date: 2026-08-05

## Summary

Patch release for finalizing the native repository-intelligence engine layout, complete safe project file-tree graph coverage, and clean Codex runtime handoff metadata.

## Changed

- Completed the move from the misleading `vendor/repository-intelligence-engine` location into the owned `engines/repository-intelligence` runtime engine folder.
- Kept upstream attribution and modification metadata inside the engine folder.
- Added complete safe project file-tree nodes to the repository graph so non-AST files can still be searched and opened from `graph.html`.
- Kept generated `.ueef`, ignored `vendor`, `.git`, dependency folders, build outputs, caches, and sensitive filenames out of the file-tree graph layer.

## Verification

- `scripts/test-repository-intelligence.ps1`
- `scripts/test-repository-intelligence-viewer.mjs`
- `scripts/test-release-consistency.ps1`
- `scripts/validate-task-evidence.ps1`
- `scripts/validate-completion-audit.ps1`
- `scripts/validate-framework.ps1`
