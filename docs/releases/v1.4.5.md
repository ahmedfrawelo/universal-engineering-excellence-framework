# UEEF v1.4.5

Patch release completing Unix validation parity.

## Changes

- Unix validator now requires v1.4.4 and all current release notes.
- Unix validator checks `VERSION.md` against `release-manifest.json`.
- WSL was detected but no Linux distribution is installed, so shell runtime execution remains an explicitly reported environment limitation.
