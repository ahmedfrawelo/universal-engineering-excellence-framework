# UEEF 2.25.4

Release date: 2026-08-04

## Summary

This patch release supersedes `v2.25.3` after GitHub Actions exposed a UTF-8 BOM in release metadata. It preserves the completed 2.25.x runtime-enforcement payload, the offline viewer asset normalization fix, and the Unix route-test alignment.

## Fixed

- Rewrote release metadata files as UTF-8 without BOM so Unix JSON parsing accepts `release-manifest.json`.
- Kept `scripts/test-agent-route.sh` aligned with the current PowerShell route contract.
- Kept `vis-network.min.js` marked as `-text` so its pinned SHA384 remains stable across Windows and Unix checkout.

## Verification

- `scripts/test-release-consistency.ps1` passed for `2.25.4`.
- Full framework validation must pass on GitHub Actions before publishing the GitHub Release object.
