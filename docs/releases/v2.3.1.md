# UEEF v2.3.1

Added browser-control runtime health diagnostics. When Chrome extension and native-host checks pass but Codex cannot start the control channel, the agent must identify that internal runtime failure, preserve the user's browser session, and guide recovery without mislabeling it as a missing tab or login problem.
