# v1.6.0

## Workspace hygiene

- Added the Workspace Hygiene pack for logs, screenshots, caches, test reports, traces, temporary files, and reproducible build output.
- Added cross-platform cleanup scripts with dry-run defaults, retention windows, Git tracking checks, and protected paths.
- Added a quality gate and checklist requiring repository-owned, reviewable cleanup behavior.
- Included stale screenshots, `dist`/`build` output, and untracked deployment staging in the protected cleanup workflow.
