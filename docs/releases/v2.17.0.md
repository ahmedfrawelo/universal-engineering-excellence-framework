# UEEF 2.17.0

Release date: 2026-07-23

- Added `test-intent-fidelity-contract.sh` and Ubuntu CI coverage for the same critical intent regressions guarded on Windows: hard `medium` caps, universal caps, mandatory spawning, and mandatory child evidence for `T1`.
- Reduced generated runtime `AGENTS.md` from the prior 3,332-character ritual copy to a compact precedence-first block (under 2,200 characters), with canonical detail retained in `UEEF-LOADER.md` and selected modules.
- Made visible Windows browser control explicitly Windows-only. On macOS/Linux, an unavailable plugin surface stops and requests the existing user-owned tab instead of inventing a fallback.
- Added behavioral JSON fixtures for explain, one-file edit, and strict-scope requests; documented honest Unix `UNSUPPORTED_ON_UNIX` health scope; and added optional `ueef-doctor.ps1` runtime/drift guidance.
