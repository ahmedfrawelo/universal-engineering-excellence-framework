# UEEF v2.4.1

When the user explicitly asks to open a site, agents may open a new tab in the existing user-owned Chrome window and profile after platform authorization. They must not create a separate browser, profile, or isolated session.
