# Continuous Assurance Gate

Pass only when the repository audit is reproducible, read-only by default, cross-platform where supported, and covers validation, scripts, security hygiene, generated artifacts, release parity, and runtime integrity.

Fail on silent mutation, secret exposure, tracked generated output, version drift, syntax errors, missing required files, or a green summary that hides failed checks.
