# Application Shell Design

Reusable design and engineering contracts for analyzing and building an application's shared shell: sidebar, header, navigation, page regions, transitions, responsive behavior, and loading states.

Select this pack whenever a task changes the global frame or a page's relationship to it. Start from the existing shell and theme evidence; do not invent a parallel shell per feature.
