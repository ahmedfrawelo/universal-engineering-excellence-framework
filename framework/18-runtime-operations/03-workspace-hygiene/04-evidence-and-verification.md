# Evidence And Verification

Cleanup output must report the root, cutoff, candidate count, and each candidate. After applying, rerun the dry run and confirm that protected paths and tracked files were not changed. Check `git status` and retain test or release evidence that is still needed.
