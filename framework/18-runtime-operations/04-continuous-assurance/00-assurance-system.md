# Continuous Assurance System

Run `scripts/ueef-audit.ps1` on Windows or `scripts/ueef-audit.sh` on Unix at release boundaries and after changes to loaders, scripts, gates, manifests, installers, or runtime behavior. Keep the default audit read-only and make failures actionable.
