# Audit Scope

The baseline audit covers required files and non-empty guidance, Markdown and pack counts, master-index links, runtime fields, script syntax, whitespace errors, sensitive-looking files, tracked generated artifacts, and version/manifest parity.

Task-specific security, dependency, build, accessibility, performance, and end-to-end checks remain required when the changed surface needs them.
