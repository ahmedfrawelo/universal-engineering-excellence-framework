# Recommendation Policy

When evidence is missing, recommend a small governed role rather than a page-local literal. Every recommendation must state why the existing system cannot satisfy it, the proposed token/asset/library, accessibility and theme impact, bundle/runtime impact, migration scope, owner, and review expiry.

Recommendations must distinguish `extracted`, `inferred`, and `proposed`. Inferred values need confirmation from consumers; proposed values never silently become implementation.
